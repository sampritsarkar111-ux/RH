# Frontier 48 — exact incomplete-Gamma derivative recurrence and transformed coefficient structure

## Objective

Push the exact theta double-sum representation one level further by eliminating the high logarithmic moments `I_N(a)` in favor of the single base function `I_0(a)=a^{-1/4} Gamma(1/4,a)` and explicit rational polynomials. This gives an exact finite-dimensional representation of every atom `B_m(k)` and identifies a new route for proving the required quadratic-form inequalities.

## 1. Base ODE

Define
\[
I_0(a)=\int_1^\infty t^{-3/4}e^{-at}\,dt=a^{-1/4}\Gamma(1/4,a),\qquad a>0.
\]
Differentiating the incomplete-Gamma expression gives
\[
\boxed{I_0'(a)=-\frac{1}{4a}I_0(a)-\frac{e^{-a}}a.}
\]
Since
\[
I_N(a)=(-\partial_a)^N I_0(a),
\]
all logarithmic moments are generated by this first-order ODE.

## 2. Exact derivative decomposition

There exist rational functions `P_N,Q_N` such that
\[
\boxed{I_N(a)=P_N(a)I_0(a)+Q_N(a)e^{-a}.}
\]
Starting from `P_0=1,Q_0=0`, the exact recurrence is
\[
\boxed{
P_{N+1}=-P_N'+\frac{P_N}{4a},
\qquad
Q_{N+1}=-Q_N'+Q_N+\frac{P_N}{a}.
}
\]
The first coefficient has the closed form
\[
\boxed{P_N(a)=\frac{(1/4)_N}{a^N}>0.}
\]
For example,
\[
Q_1=\frac1a,
\quad
Q_2=\frac{4a+5}{4a^2},
\quad
Q_3=\frac{16a^2+36a+45}{16a^3},
\]
\[
Q_4=\frac{64a^3+208a^2+468a+585}{64a^4}.
\]
Thus every `I_N` is explicitly reduced to `I_0` plus an elementary exponential term.

## 3. Exact transformed atom `B_m`

Recall
\[
B_m(a):=16m(2m-1)I_{2m-2}(a)-I_{2m}(a).
\]
Substitution of the recurrence gives
\[
\boxed{B_m(a)=R_m(a)I_0(a)+S_m(a)e^{-a},}
\]
where
\[
R_m(a)=16m(2m-1)\frac{(1/4)_{2m-2}}{a^{2m-2}}-\frac{(1/4)_{2m}}{a^{2m}},
\]
\[
S_m(a)=16m(2m-1)Q_{2m-2}(a)-Q_{2m}(a).
\]
For the first cases:
\[
R_1(a)=\frac{256a^2-5}{16a^2},
\qquad
S_1(a)=-\frac{4a+5}{4a^2},
\]
\[
R_2(a)=\frac{15(512a^2-39)}{256a^4},
\]
\[
S_2(a)=\frac{6080a^3+7472a^2-468a-585}{64a^4},
\]
\[
R_3(a)=\frac{1755(1280a^2-119)}{4096a^6}.
\]
For every theta atom, `a=pi k^2 >= pi`, so the displayed `R_m` factors are already positive for `m=1,2,3`, and the same positivity follows immediately for general `m` once the elementary bound below is checked.

## 4. A useful general positivity fact for `R_m`

Using
\[
\frac{(1/4)_{2m}}{(1/4)_{2m-2}}=(2m-\tfrac54)(2m-\tfrac74),
\]
we obtain
\[
R_m(a)=16m(2m-1)\frac{(1/4)_{2m-2}}{a^{2m-2}}
\left[1-\frac{(2m-\tfrac54)(2m-\tfrac74)}{16m(2m-1)a^2}\right].
\]
Therefore `R_m(a)>0` whenever
\[
a^2>\frac{(2m-\tfrac54)(2m-\tfrac74)}{16m(2m-1)}.
\]
The right side is < 1/4 for every `m>=1`, while every theta atom satisfies `a=pi k^2>3`; hence
\[
\boxed{R_m(\pi k^2)>0\quad\text{for all }m\ge1,k\ge1.}
\]
This is a rigorous structural simplification, not numerical evidence.

## 5. New proof target: eliminate the exponential correction globally

The remaining sign issue is entirely contained in
\[
B_m(\pi k^2)=R_m(\pi k^2)I_0(\pi k^2)+S_m(\pi k^2)e^{-\pi k^2}.
\]
For low `m`, `S_m` changes sign only after a finite threshold in `a`; for large `a`, the positive `R_m I_0` term dominates. A rigorous proof of a uniform inequality of the form
\[
I_0(a)\ge C_m(a)e^{-a}
\]
with explicit `C_m` could therefore certify `B_m(\pi k^2)>0` and potentially stronger ratio inequalities.

However, positivity of each `B_m` alone is not sufficient for RH. The decisive next object remains the exact quadratic form
\[
T_m=C^2q_{m+1}^2\sum_{k,\ell\ge1}
\left[B_{m+1}(k)B_{m+1}(\ell)
-\eta_m(B_m(k)B_{m+2}(\ell)+B_m(\ell)B_{m+2}(k))\right].
\]
The new route is to substitute the `R_m I_0+S_m e^{-a}` decomposition and seek a finite sum of Gram-type forms in the two atom vectors
\[
(I_0(\pi k^2))_{k\ge1},\qquad(e^{-\pi k^2})_{k\ge1}.
\]

## 6. Proof-audit status

Established exactly:
- the first-order ODE for `I_0`;
- the finite recurrence for all `I_N`;
- the closed form `P_N=(1/4)_N a^{-N}`;
- an exact `I_0/e^{-a}` decomposition of every `B_m`;
- strict positivity of the `I_0` coefficient `R_m(pi k^2)` for every `m,k`.

Not established:
- positivity of every `B_m`;
- the Turan quadratic form for every `m`;
- the degree-3 cone `4v^3+w^2<=0`;
- all-degree Jensen hyperbolicity;
- RH.

**Status: rigorous new reduction; no RH proof claimed.**
